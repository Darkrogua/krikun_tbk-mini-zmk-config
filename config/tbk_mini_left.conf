#
# TBK Mini - left half (PERIPHERAL, connects to dongle central)
#
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n

# Optional: don't enumerate as USB keyboard when plugged in (still flashable via bootloader)
CONFIG_ZMK_USB=n


